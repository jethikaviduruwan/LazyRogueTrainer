#include "LRT/Math/Transform.h"

// Transform is primarily header-only (constexpr / inline).
// This translation unit exists to guarantee the symbol is compiled.
namespace LRT::Math {
    // Reserved for future non-inline methods.
}
